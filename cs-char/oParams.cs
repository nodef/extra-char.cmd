using System;

namespace cs_char {
	class oParams {

		// data
		/// <summary>
		/// Indicates that the ASCII value of input is required.
		/// </summary>
		public bool ascii;
		/// <summary>
		/// Indicates that both the input and output must be shown, side by side.
		/// </summary>
		public bool verbose;
		/// <summary>
		/// Defines input string that is to be using instead of stdin.
		/// </summary>
		public string input;
	}
}
